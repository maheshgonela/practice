import 'package:flutter/material.dart';

class NewRTVShipment extends StatelessWidget {
  const NewRTVShipment({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
